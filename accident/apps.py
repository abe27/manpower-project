from django.apps import AppConfig


class AccidentConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'accident'
    verbose_name = u'จัดการข้อมูลบันทึกข้อมูลการเกิดอุบัติเหตุ'
