from django.apps import AppConfig


class TimeattendanceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'timeattendance'
    verbose_name = u'จัดการข้อมูลบันทึเวลาเข้าทำงาน'
